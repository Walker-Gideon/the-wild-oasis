/**\*** React Query
you install React query as
npm i @tanstack/react-query

with the version  
npm i @tanstack/react-query@(version number)

Next you set it up in the app.js file
Check the dock

but we can install the dev too as
npm i @tanstack/react-query-devtools

after installation, we do this in the app.js file
<ReactQueryDevtools initialIsOpen={false} />
