/**\*** React Query you install React query as npm i @tanstack/react-query with
the version npm i @tanstack/react-query@(version number) Next you set it up in
the app.js file Check the dock but we can install the dev too as npm i
@tanstack/react-query-devtools after installation, we do this in the app.js file
<ReactQueryDevtools initialIsOpen="{false}" />

<!-- useQuery -->
hook useQuery({ queryKey: [] // which take in key as an id, queryFn: for
fetching the data from the api })

<!-- useMtation -->
It is used inorder to delete data useMutation({});

<!-- For toast -->
use react-hot-toast npm i react-hot-toast Check the docs for everything you need
<Toaster position="top-center" gutter={12} reverseOrder={false}
containerStyle={{ margin: "8px" }} toastOptions={{
          success: {
            duration: 3000,
          },
          error: {
            duration: 5000,
          },
          style: {
            fontSize: "16px",
            maxWidth: "500px",
            padding: "16px 24px",
            background: "var(--color-grey-0)",
            color: "var(--color-grey-700)",
          },
}}
/>

<!-- react form -->
 npm i react-hook-form